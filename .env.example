# Server-only (Vercel project env vars) — never prefix these with VITE_
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
APP_SECRET=

# Client-side (baked into the built JS bundle at build time)
VITE_APP_SECRET=
